name: Release

on:
  push:
    tags:
      - '*.*.*'

jobs:
  build-and-tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install npm dependencies
        run: npm ci

      - name: Build JS artifact
        run: npm run build

      - name: Commit built JS
        run: |
          git addm -f ui-public
          git commit -m "Include built JS for ${GITHUB_REF#refs/tags/}"

      - name: Update Git tag
        run: |
          TAG_NAME=${GITHUB_REF#refs/tags/}
          git tag -f $TAG_NAME
          git push origin $TAG_NAME --force
